const { expect } = require('chai');

/*
Name
Переменная с названием профессии

Description

Вы очень хотите поехать в отпуск на Гаваи...море...солнце...пляж... Единственные авиакомпания, которая летит на Гаваи это "Lufthansa". Вам надо создать переменную 'airline' с именем "Lufthansa".
Если будет ошибка, то оператор отправит Вас в холодный поселок Оймякон.

Приведем пример, который принципиально похож на то что нужно сделать,
но адаптируйте его к требованиям задачи:
```
const air = 'KLM';

```
*/

// Solution
const airline = 'Lufthansa';


// Tests

it('Переменная `airline` существует и имеет значение', () => {
    expect(airline).not.undefined;
});

it('Переменная `profi` содержит тип данных string', () => {
    expect(airline).a('string');
});

it('Переменная `profi` содержит значение `doctor`', () => {
    expect(airline).eq('Lufthansa');
});